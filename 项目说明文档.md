# Spatio-temporal Transformer for URFC 项目说明文档

> **论文参考**：*Urban region function classification via fusing optical imagery and social media data: A spatio-temporal Transformer interaction approach*  
> **任务类型**：城市区域功能分类 (Urban Region Function Classification, URFC) 多模态深度学习  
> **数据集来源**：[Baidu AI Studio URFC 竞赛数据集](https://aistudio.baidu.com/datasetdetail/12529)

---

## 目录
- [一、项目背景与任务定义](#一项目背景与任务定义)
- [二、环境配置与依赖安装](#二环境配置与依赖安装)
- [三、数据准备与目录结构](#三数据准备与目录结构)
- [四、网络架构与算法原理](#四网络架构与算法原理)
- [五、快速上手与运行指南](#五快速上手与运行指南)
- [六、工程重构与代码修复记录](#六工程重构与代码修复记录)
- [七、常见问题 (FAQ)](#七常见问题-faq)

---

## 一、项目背景与任务定义

城市区域功能分类（URFC）旨在识别城市不同地块的土地利用与功能属性（如居住、商业、工业等）。单一数据源往往存在局限性：
- **光学/遥感影像**：空间细节丰富，提供建筑轮廓、路网、绿化及光谱纹理信息，但在区分“外表相似但功能不同”（如住宅与写字楼）的区域时存在瓶颈。
- **人群到访时空数据 (Social Media Visit Data)**：时间分辨率高，直观反映地块上人群活动的周期性强弱（工作日 vs 节假日、白昼 vs 黑夜），能有效弥补静态影像的语义不足。

本项目提出了一种基于 **Spatio-temporal Transformer 交互机制的多模态融合框架**，通过双分支特征抽取、跨模态双向注意力交互与辅助多任务监督，实现高精度的城市功能区域 9 分类。

### 9 种功能分类标签对照表
| 类别编码 (Target) | 功能类别说明 | 典型特征 |
| :---: | :---: | :---: |
| 0 | 居住区 (Residential) | 早晚潮汐人流明显，夜间到访活跃 |
| 1 | 商业区 (Commercial) | 周末与节假日人流量大，午间/夜间高峰 |
| 2 | 工业区 (Industrial) | 白天稳定人流，夜间及周末活动极少 |
| 3 | 交通枢纽 (Transportation) | 全天候高密度人群出入 |
| 4 | 公共服务 (Public Services) | 学校/医院/政务，工作日规律性强 |
| 5 | 绿地与公园 (Green Space/Park) | 植被指数高，周末白天休闲人流密集 |
| 6 | 仓储物流 (Logistics/Storage) | 建筑体量大，夜间及货运周期特征 |
| 7 | 教育科研 (Educational/Research) | 校园操场建筑群，学期周期性明显 |
| 8 | 待开发/其他 (Under-developed/Other) | 建筑稀疏，人流稀少 |

---

## 二、环境配置与依赖安装

### 1. Python 环境要求
- 推荐 Python 3.8 ~ 3.12
- 推荐使用 Anaconda / Miniconda 创建虚拟环境：
```bash
conda create -n urfc python=3.10 -y
conda activate urfc
```

### 2. 安装 PyTorch (带 CUDA 支持)
根据您的显卡 CUDA 版本安装对应的 PyTorch（以 CUDA 12.1 为例）：
```bash
pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
```

### 3. 安装依赖项
在项目根目录下执行：
```bash
pip install -r requirements.txt
```

`requirements.txt` 核心包清单：
- `torch>=1.10.0`
- `torchvision>=0.11.0`
- `opencv-python>=4.5.0`
- `pandas>=1.3.0`
- `scikit-learn>=1.0.0`
- `numpy>=1.20.0`
- `tqdm>=4.62.0`
- `imgaug>=0.4.0` (可选，包含数据增强备用方案)
- `pretrainedmodels>=0.7.4` (可选兼容项)

---

## 三、数据准备与目录结构

### 1. 下载数据集
请从百度飞桨 AI Studio 下载数据集：  
🔗 [https://aistudio.baidu.com/datasetdetail/12529](https://aistudio.baidu.com/datasetdetail/12529)

### 2. 规范目录树
下载并解压后，确保项目根目录结构如下：
```text
Spatio-temporal-Transformer-for-urfc/
├── train.csv                     # 训练标注表 (包含 Id 与 Target 列)
├── test.csv                      # 测试样本表 (包含 Id 列)
├── data/
│   ├── train/                    # 训练集图像 (如 000001.jpg, 100x100x3)
│   ├── test/                     # 测试集图像 (如 040001.jpg)
│   └── npy/
│       ├── train_visit/          # 训练集到访数据 (.npy 文件，7x26x24 数组)
│       └── test_visit/           # 测试集到访数据 (.npy 文件)
├── config.py                     # 全局超参数与路径配置
├── multimodal.py                 # 多模态核心网络结构与 Dataset
├── multimain.py                  # 训练、评估与测试主脚本
├── Transformer.py                # Transformer 基础组件
├── selfattention.py              # 时空到访 -> 图像注意交互
├── selfattentiontovis.py          # 图像 -> 时空到访注意交互
├── Focalloss.py                  # 类别不均衡损失实现
├── utils.py                      # 评估指标、日志记录与模型保存
├── test_verify.py                # 本地环境与模型快速连通性验证脚本
└── requirements.txt              # 依赖清单
```

### 3. 数据规格说明
- **图像数据 (`.jpg`)**：大小为 $100 \times 100$ RGB 三通道，送入网络前会被自适应缩放至 ViT 标准输入 $224 \times 224$。
- **到访数据 (`.npy`)**：三维数组 $(7, 26, 24)$，即 7 天/周、26 周、24 小时/天，在数据集中转置并展开为 182 天 $\times$ 24 小时的连续时序特征。

---

## 四、网络架构与算法原理

整个网络结构在 [multimodal.py](file:///d:/z_task/2026/Spatio-temporal-Transformer-for-urfc/multimodal.py) 的 `MultiModalNet` 中实现：

```
                        [输入: 光学图像 100x100x3]
                                   │
                           Resize(224, 224)
                                   │
                             ViT-B/16 主干
                                   │ (输出 1000 维)
                    ┌──────────────┴──────────────┐
             [池化与重构]                   [线性降维 256维]
      (MaxPool + ConvTranspose)                   │
                    │                             ├─► [辅助分类头 1 (Loss权重 0.25)]
            融合多尺度特征 (2100维)                │
                    │                             │
             降维至 256 维 ─────────► [跨模态注意力交互] ◄───┐
                    │                      │ (Image->Vis)      │
                    │                      ▼                   │
                    │                [特征融合分支]            │
                    │                      │                   │
                    └──────────────► [拼接 (322维)] ◄──────────┤
                                           │                   │
                                   [主分类器 9 分类]           │
                                           ▲                   │
                                           │                   │
                                    [特征融合分支]             │
                                           ▲                   │
                                           │ (Vis->Image)      │
                                    [跨模态注意力交互] ─────────┘
                                           │
                                   [时序降维 64维]
                                           │
                                           ├─► [辅助分类头 2 (Loss权重 0.25)]
                                           │
                                 [ConvTransformer 模块]
                                           │
                               [输入: 到访时空数据 (24, 182)]
```

### 核心亮点
1. **ViT-B/16 多尺度特征重构**：不仅提取 ViT 顶层分类特征，还通过将特征映射后进行最大池化与转置卷积（ConvTranspose）生成多尺度表示，拼接出 2100 维复合视觉特征。
2. **ConvTransformer 时序建模拟**：通过 1D 卷积时序自注意力机制捕捉长达 182 天的日周期和周周期人群规律。
3. **双向跨模态注意力融合**：
   - 到访特征引导图像注意力（`SelfAttention`）
   - 图像特征引导到访注意力（`SelfAttentiontovis`）
4. **多任务深度监督**：
   - $\mathcal{L}_{total} = \mathcal{L}_{main} + 0.25 \times (\mathcal{L}_{img\_aux} + \mathcal{L}_{vis\_aux})$
   - 保证两个分支各自具备良好的判别力，同时促进融合特征的高层语义表达。

---

## 五、快速上手与运行指南

### 1. 快速验证环境（无需数据集）
我们为您编写了自动化测试脚本 [test_verify.py](file:///d:/z_task/2026/Spatio-temporal-Transformer-for-urfc/test_verify.py)，利用随机张量验证模型的前向传播、多 batch size 适配、反向传播与模型保存读取：
```bash
python test_verify.py
```
若输出 `ALL CHECKS PASSED!`，则说明环境配置完全正确，可以开始训练。

### 2. 修改超参数 (`config.py`)
可在 [config.py](file:///d:/z_task/2026/Spatio-temporal-Transformer-for-urfc/config.py) 中按需调整：
```python
lr = 0.0015          # 学习率
batch_size = 16      # 批大小 (显存较小可调为 8)
epochs = 30          # 训练轮数
train_data = "./data/train/"
test_data = "./data/test/"
```

### 3. 开始训练
直接运行：
```bash
python multimain.py
```
- 训练过程将自动在 `bak/<日期时间_模型名>/` 目录下生成独立的日志、权重与预测输出目录。
- 采用 9:1 随机划分验证集，实时输出 `train` 与 `val` 的 Loss、Accuracy、Macro-F1 以及混淆矩阵。
- 自动保存最佳模型：
  - `*_model_best_acc.pth.tar`（最佳准确率）
  - `*_model_best_loss.pth.tar`（最小验证损失）
  - `*_model_best_f1.pth.tar`（最佳 Macro-F1）

### 4. 测试集推断
训练完成后，脚本会自动加载验证集损失最低的检查点对测试集进行推理，并将结果保存至 `submit/` 目录：
```text
submit/Spatio-temporal Transformer_bestloss_submission.csv
```

---

## 六、工程重构与代码修复记录

针对原开源代码中存在的若干致命 Bug 和工程瓶颈，本项目已进行了以下关键修复：

### 1. 修复前向传播动态实例化导致的显存泄漏与无法学习
- **原代码问题**：在 `MultiModalNet.forward()` 内部每次执行都动态执行 `conv_transpose1 = nn.ConvTranspose2d(in_channels=b, ...).to(device)`。
  - **严重后果**：每一次 forward 都重新生成随机权重，参数**完全无法被梯度优化**，且导致 GPU 显存持续上涨甚至 OOM；此外写死 `in_channels=b` 导致测试集（batch=1）或不整除 batch 维度混乱。
- **修复方案**：将 `ConvTranspose2d`、`MaxPool2d`、`Resize` 在 `__init__()` 中作为子模块正确注册，在 `forward()` 中规整为固定通道维度运算，支持任意 batch size。

### 2. 优化训练前向计算开销（提速近 3 倍）
- **原代码问题**：在 `multimain.py` 的 `train()` 和 `evaluate()` 中，为了获取三个输出分别调用了 3 次模型前向计算：
  ```python
  output = model(images, visit)[0]
  output1 = model(images, visit)[1]
  output2 = model(images, visit)[2]
  ```
- **修复方案**：合并为单次前向解包：
  ```python
  output, output1, output2 = model(images, visit)
  ```
  单 batch 计算耗时直接降低约 66%，显存吞吐大幅提升。

### 3. 修复测试阶段对 tuple 执行 Softmax 的崩溃 Bug
- **原代码问题**：`y_pred = model(...)` 返回了三元组 `(output, output1, output2)`，随后直接调用 `F.softmax(y_pred)` 导致抛出 `TypeError: softmax(): argument 'input' must be Tensor, not tuple`。
- **修复方案**：解包主分类结果 `y_pred = outputs[0]` 后计算 `F.softmax(y_pred, dim=1)`。

### 4. 修复权重保存与加载格式不匹配
- **原代码问题**：`save_checkpoint` 中执行 `torch.save(model, filename)`（保存了整个模型对象），但在加载时使用 `model.load_state_dict(best_model["state_dict"])`，造成类型报错崩溃；且保存前未递归创建目录。
- **修复方案**：修改为保存 `state` 字典，并在保存前调用 `os.makedirs(..., exist_ok=True)` 确保路径自动生成。

### 5. 移除硬编码 GPU 编号
- **原代码问题**：硬编码 `os.environ["CUDA_VISIBLE_DEVICES"] = "2"`，在单卡（GPU 0）或无卡 CPU 环境下运行时抛出 CUDA 异常。
- **修复方案**：加入存在性判定与默认回退，优先遵循用户终端已有的环境变量设置。

### 6. 清理冗余模型实例与外部依赖解耦
- **原代码问题**：`__init__` 中反复加载了 4 份 ViT 模型与 SE-ResNeXt-101 实例（白白耗费数 G 显存）；强制 import 废弃的 `pretrainedmodels` 及 `reformer_pytorch`。
- **修复方案**：剔除未使用冗余实例；将未用到的模块加入 `try-except` 保护与降级，保障在极简干净的 Python 环境下即插即用。

---

## 七、常见问题 (FAQ)

### Q1: 训练时出现 Out of Memory (OOM) 显存不足怎么办？
**A**：进入 [config.py](file:///d:/z_task/2026/Spatio-temporal-Transformer-for-urfc/config.py)，将 `batch_size = 16` 修改为 `batch_size = 8` 或 `batch_size = 4`。

### Q2: 报错 `FileNotFoundError: [Errno 2] No such file or directory: './train.csv'`？
**A**：请确认已从百度 AI Studio 数据集下载并将 `train.csv` 和 `test.csv` 放置在项目根目录下。

### Q3: 为什么第一次运行时启动较慢？
**A**：首次运行需要从 torchvision 下载官方预训练好的 `vit_b_16` 权重（约 330MB），下载完成后将被缓存至本地，后续运行将直接载入。
